# Dashboard assembly — errors (dashboard.json NOT written)

- 2. visibility.by_model families = config families minus dropped — config minus dropped = ['claude', 'gemini', 'gpt', 'grok', 'perplexity']; by_model = ['claude', 'gemini', 'gpt']; missing = ['grok', 'perplexity'] (these families have no answers in the headline scope R1 C1; scope-aware variant [families with answers in scope] = ['claude', 'gemini', 'gpt'] → PASS)
